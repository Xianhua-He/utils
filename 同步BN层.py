import torch
from torchvision import  models
# import apex
#
# model = Resnet()
# apex.amp.initialize(model, optimizer, opt_level="o1")
# if use_apex:
#     with apex.amp.scale_loss(loss, optimizer) as scaled_loss:
#         scaled_loss.backword()
#
# model = torch.nn.SyncBatchNorm.convert_sync_batchnorm(model)
# model_name = sorted(name for name in models.__dict__ if name.islower() and not name.startswith("__") and callable(models.__dict__[name]))
# print(model_name)



